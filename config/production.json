{
  "cfg": {
    // mysql db information
    "mysql": {
      "connect": {
        "connectionLimit": 1, // DB setting
        "host": "localhost", // DB IP
        "port" : 3306, // DB port
        "user" : "root", // DB ID
        "password" : "password" // DB password
      }
    },
    "env": "development"
  }
}
