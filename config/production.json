{
  "cfg": {
    // mysql db information
    "mysql": {
      "connect": {
        "connectionLimit": 1, // DB setting
        "host": "", // DB IP
        "port" : 3306, // DB port
        "user" : "", // DB ID
        "password" : "" // DB password
      }
    },
    "env": "development"
  }
}
